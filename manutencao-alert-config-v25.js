(()=>{'use strict';if(window.__TDNGO_ALERT_CONFIG_V26__)return;window.__TDNGO_ALERT_CONFIG_V26__=true;
const API='https://nsbhhmrhzkqkaoznaeif.supabase.co/functions/v1/tdngo-manutencao-alert-api';let rows=[],canEdit=false,loading=false;
function token(){try{return JSON.parse(sessionStorage.getItem('fhsl_session')||localStorage.getItem('fhsl_session')||'{}').tdngoToken||''}catch{return''}}
async function post(action,p={}){const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+token()},body:JSON.stringify({action,...p}),cache:'no-store'});const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw new Error(d.message||'Falha ao carregar alertas.');return d}
function cfg(pri){return rows.find(x=>x.prioridade===pri)||{prioridade:pri,atraso_inicial_seg:0,repetir_cada_seg:300,max_repeticoes:1,som:'PERSONALIZADO',volume_percent:100,ativo:true}}
window.tdngoGetAlertConfig=(pri='P3')=>cfg(pri);
function css(){if(document.getElementById('alert25-css'))return;const s=document.createElement('style');s.id='alert25-css';s.textContent='.alert25{grid-column:1/-1}.alert25-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px}.alert25-pri{border:1px solid var(--border);background:var(--surface2);border-radius:12px;padding:11px}.alert25-pri h4{margin:0 0 8px;font-size:13px}.alert25-pri label{display:block;font-size:9px;color:var(--muted);text-transform:uppercase;margin:8px 0 3px}.alert25-pri input,.alert25-pri select{width:100%}.alert25-note{font-size:10px;color:var(--muted);line-height:1.45;margin-top:8px}.alert25-actions{display:flex;justify-content:flex-end;gap:7px;margin-top:12px}.alert25-status{display:none;margin-top:12px;padding:10px 12px;border-radius:10px;font-size:11px;font-weight:750;line-height:1.45}.alert25-status.ok{display:block;background:#edf9f1;border:1px solid #a8d8b5;color:#23683b}.alert25-status.err{display:block;background:#fff3f2;border:1px solid #efb6b2;color:#9f2f2a}.alert25-status.info{display:block;background:#eef5ff;border:1px solid #b9d3f5;color:#285a94}@media(max-width:1000px){.alert25-grid{grid-template-columns:1fr 1fr}}@media(max-width:640px){.alert25-grid{grid-template-columns:1fr}}';document.head.appendChild(s)}
function soundOpts(v){return [['PERSONALIZADO','Sirene atual'],['GRAVE','Alerta grave'],['DUPLO','Duplo toque'],['SUAVE','Alerta suave'],['SEM_SOM','Sem som']].map(([id,n])=>`<option value="${id}" ${id===v?'selected':''}>${n}</option>`).join('')}
function status(msg,type='info'){const e=document.getElementById('alert25-status');if(!e)return;e.textContent=String(msg||'');e.className='alert25-status '+type}
function formCfg(pr){const q=a=>document.querySelector(`[data-a="${a}"][data-pr="${pr}"]`);return {prioridade:pr,ativo:q('ativo')?.checked!==false,atraso_inicial_seg:Number(q('delay')?.value||0),repetir_cada_seg:Number(q('repeat')?.value||300),max_repeticoes:Number(q('max')?.value||1),som:q('som')?.value||'PERSONALIZADO',volume_percent:Number(q('vol')?.value||100)}}
function host(){return document.querySelector('#page-settings .cfg-module[data-module="alerts"] .cfg-module-grid')||document.querySelector('#page-settings .settings-grid')}
function render(){const h=host();if(!h)return false;let card=document.getElementById('alert25-card');if(!card){card=document.createElement('div');card.id='alert25-card';card.className='card settings-card alert25'}if(card.parentElement!==h)h.appendChild(card);card.innerHTML=`<div class="section-title"><h3>Notificações e sirene</h3></div><p class="small muted">Defina quando cada prioridade toca, se haverá repetição e qual som será usado. A repetição só ocorre enquanto a Central estiver aberta; com o app fechado, vale a regra de notificação do sistema operacional.</p><div class="alert25-grid">${['P1','P2','P3','P4'].map(pr=>{const c=cfg(pr);return `<div class="alert25-pri"><h4>${pr}</h4><label><input type="checkbox" data-a="ativo" data-pr="${pr}" ${c.ativo!==false?'checked':''} ${canEdit?'':'disabled'}> Alertas sonoros ativos</label><label>Atraso inicial (segundos)</label><input type="number" min="0" max="3600" step="1" data-a="delay" data-pr="${pr}" value="${c.atraso_inicial_seg}" ${canEdit?'':'disabled'}><label>Repetir a cada (segundos)</label><input type="number" min="1" max="86400" step="1" data-a="repeat" data-pr="${pr}" value="${c.repetir_cada_seg}" ${canEdit?'':'disabled'}><label>Quantidade de toques</label><input type="number" min="1" max="10" step="1" data-a="max" data-pr="${pr}" value="${c.max_repeticoes}" ${canEdit?'':'disabled'}><label>Som</label><select data-a="som" data-pr="${pr}" ${canEdit?'':'disabled'}>${soundOpts(c.som)}</select><label>Volume (%)</label><input type="number" min="0" max="100" step="1" data-a="vol" data-pr="${pr}" value="${c.volume_percent}" ${canEdit?'':'disabled'}><div class="alert25-note">${c.max_repeticoes===1?'Um único toque. O intervalo só será usado quando a quantidade de toques for maior que 1.':'O primeiro toque respeita o atraso; os demais usam o intervalo configurado.'}</div><button class="btn sm" type="button" style="margin-top:8px" onclick="tdngoTestConfiguredSiren('${pr}')">Testar ${pr}</button></div>`}).join('')}</div><div id="alert25-status" class="alert25-status"></div><div class="alert25-actions"><button class="btn" type="button" onclick="tdngoReloadAlertConfig()">Atualizar</button>${canEdit?'<button id="alert25-save" class="btn primary" type="button" onclick="tdngoSaveAlertConfig()">Salvar alertas</button>':''}</div>`;return true}
async function load(){if(loading)return;loading=true;try{const d=await post('alert_get');rows=d.data||[];canEdit=!!d.can_edit;window.tdngoAlertConfigs=rows;render()}catch(e){console.warn('[TDNGo alert config]',e);render();status(e.message||'Não foi possível carregar a configuração de alertas.','err')}finally{loading=false}}
window.tdngoReloadAlertConfig=load;
window.tdngoSaveAlertConfig=async function(){const b=document.getElementById('alert25-save');try{const out=['P1','P2','P3','P4'].map(formCfg);for(const c of out){if(!Number.isInteger(c.atraso_inicial_seg)||c.atraso_inicial_seg<0||c.atraso_inicial_seg>3600)throw new Error(`Atraso inicial inválido para ${c.prioridade}.`);if(!Number.isInteger(c.repetir_cada_seg)||c.repetir_cada_seg<1||c.repetir_cada_seg>86400)throw new Error(`Intervalo de repetição inválido para ${c.prioridade}. Use de 1 a 86400 segundos.`);if(!Number.isInteger(c.max_repeticoes)||c.max_repeticoes<1||c.max_repeticoes>10)throw new Error(`Quantidade de toques inválida para ${c.prioridade}.`);if(!Number.isInteger(c.volume_percent)||c.volume_percent<0||c.volume_percent>100)throw new Error(`Volume inválido para ${c.prioridade}.`)}if(b){b.disabled=true;b.textContent='Salvando...'}status('Salvando configuração de alertas...','info');const d=await post('alert_save',{rows:out});rows=d.data||out;window.tdngoAlertConfigs=rows;render();const p1=cfg('P1');status(`Configuração salva com sucesso. P1: intervalo ${p1.repetir_cada_seg}s, ${p1.max_repeticoes} toque(s), som ${p1.som}.`,'ok');try{window.toast?.('Configuração de alertas salva.','ok')}catch{}}catch(e){status(e.message||'Não foi possível salvar os alertas.','err');try{window.toast?.(e.message||'Não foi possível salvar os alertas.','err')}catch{}}finally{const nb=document.getElementById('alert25-save');if(nb){nb.disabled=false;nb.textContent='Salvar alertas'}}};
window.tdngoTestConfiguredSiren=async function(pr){const c=document.querySelector(`[data-a="som"][data-pr="${pr}"]`)?formCfg(pr):cfg(pr);try{const ok=await window.tdngoPlaySirenPreset?.(c.som,c.volume_percent);status(ok===false?`O navegador bloqueou o áudio de ${pr}.`:`Teste ${pr} executado com ${c.som} em ${c.volume_percent}%.`,ok===false?'err':'ok')}catch(e){status(e.message||`Não foi possível testar ${pr}.`,'err')}};
function install(){css();if(render())load();else setTimeout(()=>{render();load()},250)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install,{once:true});else install();
})();